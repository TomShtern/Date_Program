# archived-utils

Legacy UI utility source snapshots preserved as text artifacts so they remain available for reference without being treated as active Java source.

- `*.source.txt` = preserved source snapshot
- `*.archived.txt` = older archived snapshot of the same utility
