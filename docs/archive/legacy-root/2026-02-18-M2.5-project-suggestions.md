# Project Development Suggestions

**Date:** 2026-02-18
**Model:** M2.5

> ⚠️ **Alignment status (2026-03-01): Historical snapshot**
> This file reflects analysis from its original date and is not a current implementation snapshot.
> Current code-verified baseline: **116 main + 88 test = 204 Java files**, **56,482 total Java LOC / 43,327 code LOC**, `mvn test` => **983 run, 0 failed, 0 errors, 2 skipped**.

---

## Current State Summary

- **84 main Java files**, **59 test files**
- **795 tests** (3 failing due to compilation errors)
- **3 entry points**: CLI (`Main.java`), JavaFX GUI (`DatingApp.java`), REST API (`RestApiServer.java`)
- **Well-structured**: Clean architecture with core/domain, storage, CLI, and UI layers

---

## Priority 1: Fix Failing Tests (Critical)

The build is broken. Three tests reference a non-existent method:

| Test                                                          | Issue                              |
|---------------------------------------------------------------|------------------------------------|
| `RelationshipHandlerTest.acceptsFriendRequest`                | `Match.revertToActive()` undefined |
| `RelationshipTransitionServiceTest.acceptFriendZoneSuccess`   | Same                               |
| `RelationshipTransitionServiceTest.acceptFriendZoneWrongUser` | Same                               |

**Suggested fix**: Either add `revertToActive()` to `Match.java` (line ~155-169 area) or remove the method calls from the tests if this transition is no longer valid.

---

## Priority 2: Feature Gaps from Pending Plans

Based on `docs/pending-plans/`:

| Feature            | Status        | Notes                                                                                                   |
|--------------------|---------------|---------------------------------------------------------------------------------------------------------|
| **Standouts**      | Partial       | `Standout.java` exists, storage implemented in `JdbiMetricsStorage`, but no `StandoutsService` in core/ |
| **Profile Views**  | Schema exists | `profile_views` table in DB, but no tracking service                                                    |
| **Interests/Tags** | Partial       | Fields in User, but no InterestMatcher service                                                          |

---

## Priority 3: UI Completeness

The JavaFX UI has **10 FXML screens** but may need work:

| Screen            | Files                                        | Notes               |
|-------------------|----------------------------------------------|---------------------|
| Login             | `login.fxml` + `LoginController`             | Entry point         |
| Dashboard         | `dashboard.fxml` + `DashboardController`     | Main hub            |
| Matching          | `matching.fxml` + `MatchingController`       | Swipe interface     |
| Profile           | `profile.fxml` + `ProfileController`         | Profile editing     |
| Preferences       | `preferences.fxml` + `PreferencesController` | Settings            |
| Matches           | `matches.fxml` + `MatchesController`         | Match list          |
| Chat              | `chat.fxml` + `ChatController`               | Messaging           |
| Stats             | `stats.fxml` + `StatsController`             | Statistics          |
| Achievement popup | `achievement_popup.fxml`                     | Milestones          |
| Match popup       | `match_popup.fxml`                           | Match notifications |

**Suggestions**:
- Add a dedicated screen for **Standouts** (the grid view mentioned in the design doc)
- Verify all screens are fully wired to their ViewModels
- Consider adding **Profile Preview** screen

---

## Priority 4: Code Quality

| Area           | Status                                                |
|----------------|-------------------------------------------------------|
| Tests          | 60% coverage enforced, needs more edge case tests     |
| Error handling | Result records used, but some areas could be improved |
| Validation     | `ValidationService` exists but may need more rules    |
| Performance    | No caching layer (mentioned in AGENTS.md limitations) |

---

## Priority 5: Potential Enhancements

| Enhancement                      | Complexity | Value                      |
|----------------------------------|------------|----------------------------|
| **Super Like** feature           | Low        | Adds engagement            |
| **Daily swipe limits** UI        | Medium     | Complete the feature       |
| **"Who Liked Me"** screen        | Medium     | Already has handler in CLI |
| **Push notification simulation** | Medium     | In-app notifications       |
| **Activity recency**             | Low        | Show "last active"         |
| **Photo upload/management**      | High       | No real image handling     |

---

## Recommendation

1. **First**: Fix the 3 failing tests to get a green build
2. **Second**: Verify the JavaFX UI is fully functional by running it
3. **Third**: Implement the StandoutsService if the feature is desired
4. **Fourth**: Add remaining UI screens or polish existing ones
