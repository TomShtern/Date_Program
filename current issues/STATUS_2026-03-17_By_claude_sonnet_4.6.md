# Project Status & Gap Analysis — 2026-03-17

> Generated by: code investigation (grep, wc -l, mvn test)
> Source of truth: actual source files, not documentation

---

## Build & Tests

- **1,100 tests, 0 failures, 0 errors** — green build (`mvn test`)
- 1 known flaky test: `ChatControllerTest#selectionTogglesChatStateAndNoteButtonsRemainWired` (JavaFX thread race, pre-existing, passes in isolation)
- REST API integration tests **exist** — 7 files in `src/test/java/datingapp/app/api/`

---

## Refactoring Plan Status (from `docs/plans/2026-02-28-structural-refactoring-plan.md`)

| Task | Description | Status |
|------|-------------|--------|
| 1 | ProfileService data-driven scoring (`FieldCheck`, `scoreCategory`) | ✅ Done — moved to `ProfileCompletionSupport`; **note:** `scorePreferences()` (lines 265–292) still has standalone logic for location/age and does NOT call `scoreCategory()` — only `scoreBasicInfo()` and `scoreLifestyle()` do |
| 2 | MatchPreferences `LifestyleDimension` loop | ✅ Done — `LifestyleDimension` record (lines 629–632), `LIFESTYLE_DIMENSIONS` list (lines 634–664), loop in `passes()` (line 694) and `getFailedDealbreakers()` (line 728); **7 convenience predicate methods still exist** (`hasSmokingDealbreaker`, `hasDrinkingDealbreaker`, `hasKidsDealbreaker`, etc., lines 447–473) — plan said remove them but they remain |
| 3 | **AppConfig.Builder deletion (~447 LOC)** | ❌ **Not done** — Builder still exists in 671-line `AppConfig.java` |
| 4 | `AppConfig.defaults()` single-sourcing | ✅ Done — only `StorageFactory` convenience overload remains (plan said leave it) |
| 5 | `InterestMatcher` extracted to own file | ✅ Done — `core/matching/InterestMatcher.java` |
| 6 | Remove `synchronized` from `User.java` | ✅ Done — 0 occurrences remain |
| 7 | **MatchingHandler raw service cleanup** | ❌ **Not done** — 5 raw service fields still in Dependencies record |
| 8 | StatsHandler config injection via ServiceRegistry | ✅ Done — uses `ProfileUseCases` from `ServiceRegistry` |

**Two tasks remain: Task 3 and Task 7.**

---

## Task 7 Deep-Dive — Raw Services Still in MatchingHandler

`src/main/java/datingapp/app/cli/MatchingHandler.java` — file is **890 lines**. The `Dependencies` record (lines ~96–110) has **8 raw service parameters** plus use-case fields:

**Full Dependencies record (verified):**
```java
public static record Dependencies(
    MatchingService matchingService,          // raw
    RecommendationService dailyService,       // raw
    UndoService undoService,                  // raw
    MatchQualityService matchQualityService,  // raw
    UserStorage userStorage,                  // raw
    ProfileService achievementService,        // raw (named achievementService)
    AnalyticsStorage analyticsStorage,        // raw
    RecommendationService standoutsService,   // raw
    AppConfig config,                         // config — injected (Task 4 done)
    AppSession userSession,
    InputReader inputReader,
    Runnable profileCompleteCallback,
    MatchingUseCases matchingUseCases,        // use-case layer ✅
    SocialUseCases socialUseCases)            // use-case layer ✅
```

**Instance variables stored from raw services (lines 63–67):**
Only 5 of the 8 raw services are stored as fields: `dailyService` (63), `undoService` (64), `userStorage` (65), `analyticsStorage` (66), `standoutsService` (67). `matchingService`, `matchQualityService`, and `achievementService` from the Dependencies record are presumably used only to construct the presenter or use-cases and not stored.

**Where raw services are actually called in the body:**

| Raw Service | Line(s) | Call | Status in MatchingUseCases |
|-------------|---------|------|---------------------------|
| `analyticsStorage` | 224 | `recordProfileView(currentUser.getId(), candidate.getId())` | ❌ Not exposed — no equivalent in MatchingUseCases |
| `userStorage` | 374, 468, 502, 752, 774 | `get(otherUserId)` — for unmatch, block, friend request, match detail flows | ❌ Not exposed — `listActiveMatches()` returns a `usersById` map but specific CLI paths call storage directly |
| `dailyService` | ~534–535 | `getStatus(userId)`, `getTimeUntilReset()` | ✅ Already exposed — anonymous `DailyLimitService` impl inside `wrapDailyLimitService()` (MatchingUseCases lines 513–550) |
| `undoService` | ~598–607 | `canUndo()`, `getSecondsRemaining()`, `undo()` | Partial — `undoSwipe()` (MatchingUseCases line 299) calls `canUndo()` at line 308 and returns a failure result; but CLI calls `undoService.canUndo()` + `getSecondsRemaining()` directly to *display* remaining time before attempting — this pre-display path has no use-case equivalent |
| `standoutsService` | ~653 | `resolveUsers(standouts)` | ✅ Moot — `StandoutsResult` already contains `usersById` |

**To fully complete Task 7, `MatchingUseCases` needs:**
1. A `recordProfileView(userId, candidateId)` method wrapping `analyticsStorage`
2. A user-lookup by ID (or enrich existing query results to cover all CLI paths)
3. Expose daily status / undo-status queries so the CLI pre-check calls can route through use cases

---

## Code Quality Issues Found in Source

### 1. MatchPreferences — Convenience Dealbreaker Methods Not Removed

**File:** `src/main/java/datingapp/core/profile/MatchPreferences.java` lines 447–473

The refactoring plan (Task 2) explicitly said: *"Remove `hasSmokingDealbreaker()` etc. convenience methods"*. They still exist: `hasSmokingDealbreaker()`, `hasDrinkingDealbreaker()`, `hasKidsDealbreaker()`, `hasLookingForDealbreaker()`, `hasEducationDealbreaker()`, `hasHeightDealbreaker()`, `hasAgeDealbreaker()`. The loop-based evaluation was added correctly, but the old convenience methods were not pruned. Minor issue — they don't break anything but contradict the plan.

---

### 2. `ProfileService.legacyAchievementService()` — Instantiation Anti-Pattern

**File:** `src/main/java/datingapp/core/profile/ProfileService.java` lines 185–188

```java
private DefaultAchievementService legacyAchievementService() {
    return new DefaultAchievementService(
            config, analyticsStorage, interactionStorage, trustSafetyStorage, userStorage, this);
}
```

Called by **5 methods** (`checkAndUnlock`, `getUnlocked`, `getProgress`, `getProgressByCategory`, `countUnlocked`). Creates a brand-new `DefaultAchievementService` on every invocation. Should be initialized once as a `final` field in the constructor.

### 3. AppConfig.Builder — 447 LOC to Delete

**File:** `src/main/java/datingapp/core/AppConfig.java`
- Total: **672 lines**
- `public static AppConfig defaults()` at line 210 — currently calls `builder().build()`, i.e. still uses the Builder
- Builder class starts at **line 224**, ends at **line 670** — 447 lines
- **None** of the four sub-records (`MatchingConfig`, `ValidationConfig`, `AlgorithmConfig`, `SafetyConfig`) have `defaults()` factory methods yet
- The Builder's `build()` method delegates to `buildMatchingConfig()`, `buildValidationConfig()`, `buildAlgorithmConfig()`, `buildSafetyConfig()` — these become the templates for the new `defaults()` methods

**To complete Task 3:**
1. Add `defaults()` to each sub-record, extracting values from the corresponding `buildXxxConfig()` builder method
2. Change `AppConfig.defaults()` to construct sub-records directly: `return new AppConfig(MatchingConfig.defaults(), ...)`
3. Delete lines 224–670 (the entire Builder class)

---

## Feature / UI Status

### Items from `project_improvement_plan.md` — Verified Against Source

| Item | Status | Evidence |
|------|--------|----------|
| Location dialog (city/ZIP, not raw lat/lon) | ✅ Implemented | `ProfileController.java` lines 1006/1010 — `citySearchField` (prompt: "Search city"), `zipField` (prompt: "Israeli ZIP code (7 digits)"), `countryCombo`; wrapped in `LocationDialogRefs` record |
| Photo 6-limit + count label (`Photos X/6`) | ✅ Implemented | `LocalPhotoStore.MAX_PHOTOS = 6`; label at `ProfileController.java` line 728: `"Photos " + count + "/" + viewModel.getMaxPhotos()` |
| Chat polling / auto-refresh | ✅ Implemented | `ChatViewModel` — `runPolling("chat-conversations-polling", conversationPollInterval, ...)` |
| Enter to Send | ✅ Implemented | `ChatController.java` line 730 — `KeyCode.ENTER && !isShiftDown()` → calls `handleSendMessage()` |
| Presence indicator hides when UNKNOWN | ✅ Fixed | `ChatController` lines 290–311 — `boolean visible = resolvedStatus != PresenceStatus.UNKNOWN`; dot hidden when unknown |
| Unmatch button in UI | ✅ Implemented | `MatchesController` line 628 (`unmatchBtn` → `handleUnmatch()` at line 698); `ChatController` field line 96, handler line 854 |
| Friend Zone / requestFriendZone button | ✅ Implemented | `MatchesController` line 620 — `friendZoneBtn` → `handleRequestFriendZone()` → `viewModel.requestFriendZone(match)` (line 685) → `SocialUseCases.requestFriendZone()` |
| Theme persistence | ✅ Implemented | `UiPreferencesStore` — saves/loads `ThemeMode` via `java.util.prefs.Preferences` |
| Age/distance filter persistence | ✅ Implemented | `PreferencesViewModel.savePreferences()` → updates `User` via use-case → persisted to DB |
| Daily pick context handoff to matching screen | ✅ Implemented | `DashboardController.handleViewDailyPick()` sets `NavigationContext` with UUID; `MatchingController` consumes via `consumeNavigationContext(MATCHING, UUID.class)` → `viewModel.initialize(selectedCandidateId)` |
| REST API integration tests | ✅ Exist | 7 test files: `RestApiRoutesTest`, `RestApiReadRoutesTest`, `RestApiRelationshipRoutesTest`, `RestApiPhaseTwoRoutesTest`, `RestApiNotesRoutesTest`, `RestApiHealthRoutesTest`, `RestApiConversationBatchCountTest` |

### Genuinely Open Items

| Item | File | Notes |
|------|------|-------|
| Profile card view shows only **first photo** | `ProfileController.java:1563` (profile preview dialog), `ProfileViewController.java:73` | `photoUrls().getFirst()` hardcoded — no carousel; if no photos, shows placeholder icon |
| REST API has **no auth** strategy | `RestApiServer.java` | Localhost-only binding is a safeguard but auth is undocumented as dev-only |
| `Like → Match` atomicity | `JdbiMatchmakingStorage.java` | `jdbi.inTransaction()` is used at storage layer (lines 161, 392, 437, 486, 564); cross-service (storage + event bus) is by design not transactional |

---

## Priority Recommendations

| Priority | Item | Effort | Impact |
|----------|------|--------|--------|
| **P1** | Task 3: Delete `AppConfig.Builder` (lines 224–670, ~447 LOC) — first add `defaults()` to each sub-record | Medium | Largest remaining dead weight |
| **P2** | Task 7: Expose `recordProfileView` + user-lookup in `MatchingUseCases`; remove raw service fields from `MatchingHandler.Dependencies` | Medium | Completes the architectural goal of CLI handlers being thin over use-cases |
| **P3** | Task 2 cleanup: Remove 7 leftover convenience dealbreaker methods from `MatchPreferences` (lines 447–473) | Trivial | Plan said remove them; they add noise |
| **P4** | Fix `ProfileService.legacyAchievementService()` — initialize `DefaultAchievementService` as a field, not per-call | Trivial | Correctness + avoid redundant object creation |
| **P5** | Photo gallery carousel in card/profile view (currently `photoUrls().getFirst()` only) | Medium | UX — users can't see multiple photos when viewing matches |
| **P6** | REST API — document as dev-only or add minimal auth | Small | Defensive documentation |

---

## Codebase Snapshot (verified 2026-03-13)

- Java files: **267 total** (`fd -e java . src/`)
- LOC: **73,830 total / 58,653 code** (tokei)
- Test count: **1,100** (up from 820 in late Feb)
- Active DB tables: **26**
- REST endpoints: **33** (localhost-only, rate-limited 240 req/min)
